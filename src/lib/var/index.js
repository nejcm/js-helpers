/**
 * Check that value not null or undefined.
 * @param {Any} value Value to check
 * @returns {Boolean} Is value defined.
 */
export const isDefined = (value) => value !== null && value !== undefined;
